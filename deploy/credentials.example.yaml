# Copy to `$DSH_HOME/.credentials.yaml` (`~/.dsh/.credentials.yaml`).
# `scripts/setup.sh` copies this file there when the managed document is absent.
# Fill the values locally. Do not commit a file that contains real keys.

DEEPSEEK_API_KEY: ""
DEEPSEEK_PLN_API_KEY: ""
